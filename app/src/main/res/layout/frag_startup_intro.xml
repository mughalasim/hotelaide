<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <ImageView
        android:id="@+id/frag_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/height_nav_header"
        android:contentDescription="@string/app_name"
        android:padding="@dimen/padding_large"
        android:scaleType="fitCenter"
        android:tag="0"
        app:srcCompat="@drawable/img_chef" />

    <TextView
        android:id="@+id/txt_desc"
        style="@style/Text_Small"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="@dimen/padding_large"
        android:layout_marginEnd="@dimen/padding_large"
        android:layout_marginBottom="@dimen/height_collapsing_toolbar"
        android:background="@drawable/back_text_fields"
        android:elevation="@dimen/padding_mini"
        android:padding="@dimen/padding_mini"
        android:text="@string/app_name"
        android:textColor="@color/colorAccentDark"
        android:textSize="@dimen/text_size_medium"
        tools:targetApi="lollipop" />


</RelativeLayout>